{
    "name": "feedback-radar-worker",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev --remote",
        "start": "wrangler dev"
    },
    "devDependencies": {
        "wrangler": "^4.59.2"
    },
    "dependencies": {
        "cloudflare": "^5.2.0",
        "hono": "^4.0.0"
    }
}